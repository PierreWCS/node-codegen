type Query {
  getProduct(id: String!): Product
}

type Mutation {
  createProduct(args: CreateProductInput!): Product
}

type Product {
  id: String
  name: String
  description: String
}

input CreateProductInput {
  name: String
  description: String
}
